import OverviewView from "../view/OverviewView";

 /**
 * The interface for a logged in admin/recruiter
  * Can see all applications made by all users
 * @param props
 * @returns {JSX.Element}
 * @constructor
 */
export default function Overview(props) {
    //TODO send list of applications to View
    //TODO make it sortable

    //TODO ta bort TODOS

    return (
        <OverviewView/>
    )
}