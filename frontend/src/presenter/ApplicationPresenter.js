import ApplicationView from "../view/ApplicationView";

/**
 * Full view of a job application that an admin user (recruiter) can see
 * @param props
 * @returns {JSX.Element}
 * @constructor
 */
export default function Application(props) {
    //Logged in? -> access || prev page

    //Render full application - from personID?

    //"Is being modified"-check -> blocked from modification

    return (
        <ApplicationView/>
    )
}