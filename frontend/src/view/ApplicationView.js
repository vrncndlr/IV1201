export default ApplicationView;

function ApplicationView(props) {

    return (
        <div>
            //Current application
            //  Full name
            //  Status (accepted | rejected | unhandled)
            <select>Status</select>
            <button>Back to OverviewView</button>
        </div>
    )
}